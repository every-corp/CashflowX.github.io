# Agent Guidelines & Repository Memory (AGENTS.md)

## Git Identity & Commit Standards
- **Author Name**: `bot`
- **Author Email**: `bot@github.com`
- When making commits or configuring Git in this repository, always ensure the local Git user and email are set as follows:
  ```bash
  git config --local user.name "bot"
  git config --local user.email "bot@github.com"
  ```
